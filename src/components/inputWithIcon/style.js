import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  inputWithIcon: {
    // flex: 0.5,
    marginBottom: 10,
    alignItems: 'center',
    flexDirection: 'row',
  },
  input: {
    height: 40,
    borderBottomWidth: 1,
    borderBottomColor: '#dedede',
    width: '90%',
  },
  errorText: {
    marginLeft: '6%',
  },
});
